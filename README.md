# Quartz v4

With a plugin that converts from logseq-style indentation to the expected input markdown, for fellow Logseq users. 

Current issues: 
- [] Fails to parse tables within sub-lists
- sometimes just doesn't parse the first bullet? see garden.arunim.fyi/adversarial-robustness
- sometimes partially parses the first bullet? see garden.arunim.fyi/fog-of-war-chess
- sometimes doesn't separate the first and second bullet? see garden.arunim.fyi/morale
- other times it is completely fine? see garden.arunim.fyi/ai-progress
